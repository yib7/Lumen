# Normalize text line endings on commit; keep shell scripts LF so they run on
# Linux/macOS CI regardless of a contributor's local git settings.
* text=auto
*.sh   text eol=lf
*.scene text eol=lf

# Treat rendered samples as binary (no line-ending or diff munging).
*.png binary
*.gif binary
